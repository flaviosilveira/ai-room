enable-pre-post-scripts=true

# Without this, a pnpm major upgrade leaves node_modules out of sync and any
# `pnpm run` aborts with ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY in CI,
# scripts, bootstrap and service managers. Let the resync happen unattended.
confirm-modules-purge=false
